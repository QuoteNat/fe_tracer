use std::fs::File;
use Vector::Vec3D;

mod Framebuffer {
    struct Framebuffer {
        height: i32;
        width: i32;
        buffer: Vec<Vec::Vec3D>;
    }

    impl Framebuffer {
        
    }
}